{% extends 'layout.html.twig' %}

{% block title %}{{ parent() }} Créer un compte{% endblock %}

{% block section %}
    <div class="container mt-3">
    	<div class="row">
    		<div class="col-6 bg-white p-3 shadow-sm mx-auto">
    			<div class="h3">Créer un compte</div>

			    {{ form_start(registrationForm) }}
					{{ form_widget(registrationForm) }}
					<br>
			        <button class="btn btn-primary btn-lg">S'incrire</button>
			    {{ form_end(registrationForm) }}
    		</div>
    	</div>
    </div>
{% endblock %}
